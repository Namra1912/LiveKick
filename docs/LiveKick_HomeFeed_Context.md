# LiveKick — Project Context & Handoff Doc
*Generated by reading the live repo at github.com/Namra1912/LiveKick (branch: main) on Aug 6, 2026. Paste this whole doc into any AI chat to get it fully up to speed before asking for changes.*

---

## 1. What LiveKick is

A dark-themed, "Operate mode" football (soccer) live-scores + fan-engagement dashboard. The core job of the app is letting a user scan match info fast — scores, live status, standings, transfers, predictions, tactics — across 5 leagues (Premier League, La Liga, Champions League, Bundesliga, Serie A). It's currently **Phase 0: frontend-only with mock data**, no live API/backend wired yet (a stub Express/MongoDB backend exists in `/backend` but isn't connected to the frontend).

Planned full page set (per `docs/Designs/*.png` mockups): Home Feed (`/`), Match Detail (`/matches/:id`), Team Profile (`/teams/:id`), Player Profile (`/players/:id`), League Standings (`/standings`), Transfer Radar (`/transfers`), Fan Prediction League (`/predictions`), Tactical Lineup Lab (`/tactics`), News & Editorial (`/news`), Search Modal, Login/Signup (`/login`), Player Onboarding (`/onboarding`), User Profile & Settings (`/settings`).

**Current status:** The Home Feed (route `/`) is **100% complete** as a production-grade frontend dashboard with real crest integration, collapsible league groups, date selection & calendar popover, favoriting with localStorage persistence, scoped "Match Day Live" filtering, pressure bars, ⌘K search overlay, and a fully polished right panel (`MatchOfTheDayCard` with local kickoff formatting & shared `Crest` components, `PredictorCard` teaser widget, and `NewsList`).

---

## 2. Tech stack

- **Frontend:** React 19 + Vite 8, plain CSS (`tokens.css` design tokens + per-component CSS modules), `react-router-dom` v7, `lucide-react` icons, `oxlint` for linting.
- **Fonts:** self-hosted via `@fontsource` — Big Shoulders Display (display/scores), Inter (body), JetBrains Mono (timers/stats/numbers). Never load fonts via Google Fonts `<link>`.
- **Backend:** Express + MongoDB stub in `/backend` (`server.js`) — not currently wired to the frontend. All frontend data comes from `frontend/src/data/mockData.js`.
- **State/persistence:** No global state library (Redux/Zustand). LocalStorage is used directly for client persistence (`lk_fav_matches` for favorited matches, `lk_sb_teams` & `lk_sb_leagues` for sidebar accordion states).
- **Routing:** `frontend/src/routes/AppRouter.jsx` manages all application routes.

---

## 3. Design system ("Night-Pitch") — the anti-AI-slop rules

Defined in `frontend/src/styles/tokens.css` and `.agents/AGENTS.md`. **Every CSS file strictly reuses these tokens:**

- **Background stack:** base `#080c11` → surface `#0d1520` → elevated `#111820` → hover `#18222d`. Hairline borders `#1e2a35`.
- **Text:** primary `#f1f5f9`, secondary `#94a3b8`, faint `#475569`.
- **Brand:** pitch green `#00B370` (primary accent), gold `#f59e0b` (reserved ONLY for Matchday Coins currency), live red `#f87171`.
- **Banned:** pure `#000` (except the retro scoreboard digit cell), pure white text, purple AI-gradients, generic slate/blue-gray defaults.
- **Radius — strictly 3 tiers:**
  - `--radius-badge: 4px` (scoreboard digits, kickoff slots, minute badges — near-sharp)
  - `--radius-card: 14px` (double-bezel cards/containers)
  - `--radius-pill: 9999px` (segmented controls, pill buttons)
- **Double-bezel card treatment:** outer rim border + `--shadow-inset` (dark inner drop) + `--shadow-card` (outer drop) = physical depth. Favorited cards get a 3px left accent border (`border-l-[3px] border-l-[#00B370]`).
- **Retro scoreboard digit:** score numbers render in individual black `#000` cells with amber glow text `#f5b942`, 4px radius, inset glow — not plain numerals in a row.
- **Restrained glows:** stadium-floodlight radial gradients belong on parent containers only, never on individual cards.
- **Typography:** Big Shoulders Display = scores/headings (uppercase, tight tracking). Inter = body. JetBrains Mono = all numbers/timers/stats.

---

## 4. Current File Map

```
frontend/src/
├── pages/
│   ├── HomeFeed.jsx / .css          — main dashboard shell: center feed + right panel, date filter & favoriting logic
│   ├── MatchDetail.jsx              — match detail stub page
│   ├── TeamDetail.jsx               — team profile stub page
│   ├── Standings.jsx                — league standings stub page
│   ├── Transfers.jsx                — transfer radar stub page
│   ├── PredictionsLeague.jsx        — fan prediction league full page
│   ├── TacticsLab.jsx               — tactical lineup lab stub page
│   ├── News.jsx                     — news editorial stub page
│   ├── Settings.jsx                 — user settings stub page
│   └── Login.jsx / .css             — login & auth stub page
├── components/layout/
│   ├── AppLayout.jsx / .css           — TopNav + Sidebar + content 3-column layout shell
│   ├── TopNav.jsx / .css              — logo icon, search trigger (⌘K), coin balance pill, bell, avatar
│   └── Sidebar.jsx / .css             — fixed nav links, collapsible My Teams accordion, collapsible My Leagues accordion, Match Day Live toggle
├── components/feed/
│   ├── DateSelector.jsx / .css        — Yesterday/Today/Tomorrow pill + full calendar popover
│   ├── LeagueGroup.jsx / .css         — collapsible per-league section wrapping MatchRows
│   ├── MatchRow.jsx / .css            — single match row (crests, retro digit scores, status badge, star favoriting, pressure bar)
│   └── PressureBar.jsx / .css         — live-match possession/attack momentum bar
├── components/panels/
│   ├── MatchOfTheDayCard.jsx / .css   — right panel featured match card
│   ├── PredictorCard.jsx / .css       — right panel passive teaser widget (fan lean bar split, lock time, Crests size 44, CTA link to /predictions)
│   └── NewsList.jsx / .css            — right panel "Trending News" list with credibility badges
├── components/shared/
│   └── Crest.jsx / .css               — shared team/league logo component (real logo image + deterministic initials circle fallback)
├── components/icons/
│   ├── CoinIcon.jsx                   — custom SVG Matchday Coin icon
│   └── StarIcon.jsx                   — favoriting star icon
├── components/search/
│   └── SearchModal.jsx / .css         — ⌘K search overlay modal
├── routes/
│   └── AppRouter.jsx                  — central React Router configuration
├── styles/
│   ├── tokens.css                     — design tokens (colors, gradients, radii, typography, shadows)
│   ├── StubPage.css                   — generic layout styling for stub pages
│   └── tailwind.css                   — CSS entrypoint
└── data/
    └── mockData.js                    — teams, leagues, matches, standings, transfers, news, predictor data, current user
```

---

## 5. Completed Features & Implementation Details

- **Shared Crest Component (`Crest.jsx`)** — Renders real logo images from `logoUrl` (`mockData.js`) across leagues and teams with a graceful, color-deterministic initials-circle fallback if an image fails to load. Used in `MatchRow`, `LeagueGroup`, `Sidebar` accordions, `PredictorCard`, and `MatchOfTheDayCard`.
- **Sidebar Architecture & Scoped Live Filter** — Split into fixed top nav links, middle scrollable accordion area, and fixed bottom section. Both "My Teams" and "My Leagues" are independently collapsible accordions with state persisted in `localStorage` (`lk_sb_teams`, `lk_sb_leagues`). The "Match Day Live" toggle button is strictly scoped to the Home Feed (`location.pathname === '/'`).
- **LeagueGroup Accordions** — Smart league sections for all 5 top European leagues. Automatically opens leagues containing matches on the selected date; empty leagues remain collapsible with date-aware empty messages ("No matches today/yesterday/tomorrow").
- **Date Filtering & Calendar Popover (`DateSelector.jsx`)** — Yesterday/Today/Tomorrow quick selector synced with a full month-grid calendar popover for selecting any arbitrary date, filtering matches by local calendar date.
- **Match Favoriting** — Star toggles match favoriting with state persisted to `localStorage` (`lk_fav_matches`) and double-bezel accent styling (`border-l-[3px] border-l-[#00B370]`).
- **Match of the Day Card (`MatchOfTheDayCard.jsx`)** — Polished featured match card with shared `Crest` integration, automatic browser UTC-to-local timezone kickoff formatting via `toLocaleTimeString()`, and dynamic relative day labels ("Today", "Tomorrow", "Fri, 14 Aug").
- **PredictorCard Teaser Widget (`PredictorCard.jsx`)** — Refactored from interactive voting into a clean passive teaser:
  1. Passive fan lean percentage bar (`LIV 58%` / `42% CHE`) styled with muted saturation.
  2. Kickoff lock timing context line (`Locks at 14:15` computed from `lockAt`).
  3. Incentive hook text ("Earn coins for a correct pick").
  4. Minimal green text link ("Predict →") redirecting directly to `/predictions`.
  5. Size 44 `Crest` components for balanced matchup visual density matching `MatchOfTheDayCard`.

---

## 6. Recent Commit History

- `8f79d18`: feat(predictor): convert PredictorCard into clean passive teaser with fan lean bar and redirect to /predictions
- `ae36c20`: feat(sidebar): replace sidebar league dot with Crest component
- `a1a702f`: feat(shared): add Crest component and wire real logoUrls into leagues + teams
- `8a3596d`: feat(feed): collapsible league groups with smart defaults and full badge coverage
- `e03441b`: feat(sidebar): split into fixed regions, add My Leagues section, and accordion toggles
- `4168140`: feat(feed): lift favorite match state to HomeFeed with localStorage persistence
- `96acc8c`: docs: audit and sync DESIGN.md with tokens.css and codebase implementation
- `80e934d`: feat(feed): filter matches by local calendar date before grouping by league in HomeFeed

---

## 7. Known Gaps & Remaining Tasks

*Note: The Home Feed (`/`) itself is **100% complete** with 0 outstanding bugs or missing features. The items below represent broader platform roadmap items for other routes and system features.*

1. **Full Page Builds (Platform Roadmap):** Next phase page builds (`League Standings`, `Transfer Radar`, `Match Detail`, `Team Detail`, `Predictions League`, `Tactics Lab`) following the established Home Feed component and design token patterns.
2. **Global ⌘K Search Upgrade (Platform Roadmap):** Upgrade `SearchModal` to support URL search param handoffs (e.g. `/standings?league=pl`), player search, and team-vs-team fixture search.
3. **Interactive News Navigation:** Connect `NewsList` items on the right panel to deep-link to `/news` or an article modal when clicked.
4. **Backend API Integration (Phase 1):** Connect the stub Express/MongoDB backend (`/backend`) with live football data providers to replace `mockData.js`.

---

## 8. Working Conventions with AI Pair-Programmer ("Antigravity")

- Changes are shipped as **small, scoped prompts** targeting specific files.
- All code follows the **"Night-Pitch" design system** tokens in `tokens.css` and `.agents/AGENTS.md`.
- No pure `#000` (except retro scoreboard digits), no pure white text, no purple AI gradients.
- Micro-interactions use smooth transitions and respect `prefers-reduced-motion`.
- Maintain FotMob scanning speed with LiveKick original visual identity.

